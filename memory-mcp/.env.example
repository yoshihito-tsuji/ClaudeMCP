# Memory MCP Server Configuration

# Path to store ChromaDB data (default: ~/.claude/memories/chroma)
# MEMORY_DB_PATH=/home/user/.claude/memories/chroma

# Collection name for memories (default: claude_memories)
# MEMORY_COLLECTION_NAME=claude_memories

# === Sensory Buffer Settings (Phase 1) ===
# Sensory buffer TTL (seconds) - how long sensory data persists before auto-deletion
SENSORY_TTL_SEC=60

# Sensory buffer max entries - oldest entries removed first when exceeded
SENSORY_MAX_ENTRIES=100

# === Memory Model V2 (Phase 2) ===
# Enable new memory model with short-term memory and auto-promotion
# Set to "true" to enable, "false" to use original model (default: false for compatibility)
MEMORY_MODEL_V2=false

# Short-term memory TTL (seconds) - intermediate storage before long-term (default: 3600 = 1 hour)
SHORTTERM_TTL_SEC=3600

# Short-term memory max entries - FIFO eviction when exceeded (default: 50)
SHORTTERM_MAX_ENTRIES=50

# Auto-promotion threshold - memories with importance >= this value are automatically promoted to long-term (default: 4)
AUTO_PROMOTE_THRESHOLD=4

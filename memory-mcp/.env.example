# Memory MCP Server Configuration

# Path to store ChromaDB data (default: ~/.claude/memories/chroma)
# MEMORY_DB_PATH=/home/user/.claude/memories/chroma

# Collection name for memories (default: claude_memories)
# MEMORY_COLLECTION_NAME=claude_memories

# === Sensory Buffer Settings (Phase 1) ===
# Sensory buffer TTL (seconds) - how long sensory data persists before auto-deletion
SENSORY_TTL_SEC=60

# Sensory buffer max entries - oldest entries removed first when exceeded
SENSORY_MAX_ENTRIES=100
